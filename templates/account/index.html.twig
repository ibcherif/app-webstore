{% extends 'base.html.twig' %}

{% block title %}Mon compte {% endblock %}

{% block content %}
    <h1>Mon Compte</h1>
    Bienvenu {{ app.user.firstname}} dans votre compte. <br/>
    {% if is_granted('ROLE_ADMIN') %}
        <small>
            <a href="/admin">Vous êtes un administrateur, cliquer ici pour accéder au back-office</a> <br/>
        </small>
    {% endif %}
    C'est dans cet espace que vous allez pouvoir gérer toutes vos informations personnelles.
    <hr>
    <div class="row">
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/password.png') }}" alt="Modifier mon mot de passe"><br/>
                <a href="{{ path('app_account_password') }}">Modifier mon mot de passe </a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/contact-book.png') }}" alt="Gérer ùes adresses"><br/>
                <a href="{{ path('app_account_address') }}">
                    Gérer mes adresses
                </a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/purchase.png') }}" alt="Modifier mon mot de passe"><br/>
                <a href="{{ path('app_account_order') }}">Mes commandes</a>
            </div>
        </div>
    </div>
    <hr>

{% endblock %}
