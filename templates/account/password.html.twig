{% extends 'base.html.twig' %}

{% block title %}Modification du mot de passe{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe</h1>
    <a href="{{ path('app_account') }}">Retouner à mon compte </a><br/>
    <hr>
    {% if notif  %}
        <div class="alert alert-info">{{ notif }}</div>
    {% endif %}
    {{ form(form) }}
    <hr>
{% endblock %}
